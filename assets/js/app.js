
const $=(s,r=document)=>r.querySelector(s);const $$=(s,r=document)=>Array.from(r.querySelectorAll(s));
function base(){const p=location.pathname;return p.endsWith('/')?p:p.replace(/[^/]+$/,'')} // for relative pages
function loadJSON(path){return fetch(path).then(r=>r.ok?r.json():Promise.reject(r.status))}
$('#menuBtn')?.addEventListener('click',()=>$('#navLinks')?.classList.toggle('open'));
function renderSocials(){const box=$('[data-socials]');if(!box)return;loadJSON((location.pathname.split('/').length>3?'../':'')+'data/social-links.json').then(d=>{box.innerHTML=d.map(x=>`<a class="btn" href="${x.url}" target="_blank" rel="noopener">${x.name}</a>`).join('')}).catch(()=>{})}
renderSocials();
function riskCalc(){const out=$('#riskOut');if(!out)return;const bal=parseFloat($('#balance').value)||0,risk=parseFloat($('#risk').value)||0,stop=parseFloat($('#stop').value)||0,pip=parseFloat($('#pipvalue').value)||10;const amt=bal*risk/100;const lots=stop>0&&pip>0?amt/(stop*pip):0;out.innerHTML=`Risk amount: <strong>$${amt.toFixed(2)}</strong><br>Suggested lot estimate: <strong>${lots.toFixed(2)}</strong><br><span class="muted">Educational estimate only. Confirm contract size and broker specifications.</span>`}
['balance','risk','stop','pipvalue'].forEach(id=>$('#'+id)?.addEventListener('input',riskCalc));riskCalc();
function candleRead(){const out=$('#candleOut');if(!out)return;const dir=$('#barDirection').value,spread=$('#spread').value,vol=$('#volume').value,close=$('#close').value,bg=$('#background').value;let text='Read the bar in context. '; if(dir==='up'&&spread==='narrow'&&vol==='low'&&bg==='weak') text='Possible No Demand: a narrow up bar on low volume in weak background. Wait for confirmation.'; else if(dir==='down'&&spread==='narrow'&&vol==='low'&&bg==='strong') text='Possible No Supply: a narrow down bar on low volume in strong background. Wait for confirmation.'; else if(dir==='down'&&vol==='ultra'&&close!=='low') text='Possible Stopping Volume: heavy activity with poor downside result. Wait for test/confirmation.'; else if(dir==='up'&&vol==='ultra'&&close!=='high') text='Possible supply entering: high activity but the bar fails to close strongly. Check resistance/background.'; out.textContent=text}
['barDirection','spread','volume','close','background'].forEach(id=>$('#'+id)?.addEventListener('change',candleRead));candleRead();
async function quiz(){const box=$('#quizBox');if(!box)return;const data=await loadJSON('../data/vsa-quizzes.json').catch(()=>[]);box.innerHTML=data.map((q,i)=>`<div class="card"><h3>Question ${i+1}</h3><p>${q.q}</p>${q.options.map(o=>`<button class="quiz-option" data-answer="${q.answer}" data-explain="${q.explain}">${o}</button>`).join('')}<div class="muted quizExplain"></div></div>`).join('');$$('.quiz-option').forEach(b=>b.onclick=()=>{const ok=b.textContent===b.dataset.answer;b.classList.add(ok?'correct':'wrong');b.parentElement.querySelector('.quizExplain').textContent=(ok?'Correct. ':'Review: ')+b.dataset.explain;});}
quiz();
async function searchSite(){const input=$('#siteSearch'),res=$('#searchResults');if(!input||!res)return;const files=['vsa-modules','vsa-lessons','vsa-signals','vsa-case-files','vsa-mistakes','vsa-glossary','resources','trial-classes'];let all=[];for(const f of files){try{const d=await loadJSON('../data/'+f+'.json'); all=all.concat(d.map(x=>({section:f,...x})));}catch(e){}}
input.addEventListener('input',()=>{const q=input.value.toLowerCase().trim(); if(!q){res.innerHTML='<p class="muted">Type a term such as no demand, stopping volume, test, background, or journal.</p>';return} const m=all.filter(x=>JSON.stringify(x).toLowerCase().includes(q)).slice(0,30);res.innerHTML=m.map(x=>`<div class="item"><div><strong>${x.title||x.name||x.term||x.market||x.section}</strong><p class="muted">${x.desc||x.definition||x.simple||x.lesson||x.focus||x.fix||''}</p></div><span class="pill">${x.section}</span></div>`).join('')||'<p>No result found. Add more data in JSON later.</p>';});}
searchSite();
